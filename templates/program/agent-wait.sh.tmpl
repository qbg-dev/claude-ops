#!/usr/bin/env bash
# Agent wait script — blocks on FIFO until phase gate opens.
# Auto-generated by Program API.
echo "═══ {{LABEL}} ═══"
echo "Waiting for phase gate..."
echo "Blocked on: {{FIFO_PATH}}"
echo ""
mkfifo '{{FIFO_PATH}}' 2>/dev/null || true
cat '{{FIFO_PATH}}' > /dev/null
echo "$(date '+%H:%M:%S') Gate opened. Launching {{LABEL}}..."
bash '{{WRAPPER_PATH}}'
